import React, {useState} from 'react'

function Help ({onButtonClick}) {
    const [showModal, setShowModal] = useState(false);
  return (
    <div>
      <h1>Modal</h1>
    </div>
  )
}

export default Help
